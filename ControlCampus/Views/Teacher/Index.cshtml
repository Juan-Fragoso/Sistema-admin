
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model IEnumerable<ControlCampus.Models.Teacher>

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Docentes Registrados</h2>
        <a asp-action="Create" class="btn btn-success">Registrar Docente</a>
    </div>
</div>

<table class="table table-striped table-hover border">
    <thead class="table-dark">
        <tr>
            <th>Número de empleado</th>
            <th>Nombre completo</th>
            <th>Email</th>
            <th>Teléfono</th>
            <th>Accion</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            @foreach (var teacher in Model)
            {
                <tr>
                    <td>@teacher.EmployeeNumber</td>
                    @* Accedemos a los datos de la relación User *@
                    <td>@teacher.User?.Name</td>
                    <td>@teacher.User?.Email</td>
                    <td>@teacher.Phone</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@teacher.Id" class="btn btn-warning btn-sm">
                            <i class="bi bi-pencil"></i> Editar
                        </a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="5" class="text-center text-muted">
                    No se encontraron doecentes en la base de datos.
                </td>
            </tr>
        }
    </tbody>
</table>


